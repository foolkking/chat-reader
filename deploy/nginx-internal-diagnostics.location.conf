# Public requests are deliberately concealed. Operators query the API container
# over the existing SSH + Docker boundary instead of traversing the public proxy.
location ^~ /api/internal/diagnostics {
    return 404;
    add_header Cache-Control "no-store" always;
    add_header X-Content-Type-Options "nosniff" always;
}
