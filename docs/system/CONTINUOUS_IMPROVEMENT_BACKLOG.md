# Continuous Improvement Backlog

Last reviewed: 2026-09-02
Owner: repository maintainers  
Scope: private multi-account Chat Reader Web/API/worker/deployment lifecycle.
The deployed snapshot has one deployment-provisioned administrator and isolated
user accounts; current account facts and rollout status are authoritative in
`PROJECT_STATE.md`.

This is a living candidate register, not a defect declaration. Every item keeps
its evidence level and must be revalidated before implementation. `Observed`
and `Strong` entries are grounded in current code, tests, deployment evidence,
or production diagnostics. `Inferred` entries are design or operational risks
that need a focused check. `Hypothesis` entries require measurement before
being promoted. Status is `Candidate` until a separate implementation cycle
adopts it.

Priority fields use `H/M/L`; effort and risk use `S/M/L`. Impact and urgency
are user or operator consequence, not code size.

## Candidate Register

| ID | Area | Concrete improvement | Evidence | Impact | Urgency | Confidence | Effort | Risk | Status |
|---|---|---|---|---:|---:|---|---:|---:|---|
| CI-001 | Recovery | Make `deploy/backup.sh` create and verify the database plus all five artifact archives under one timestamped manifest. | Strong: current script only dumps PostgreSQL; deployment used manual archive checks. | H | H | Strong | M | M | Completed 2026-08-31; shell-verified |
| CI-002 | Recovery | Add a dry-run restore verifier that checks archive lists, checksums, and PostgreSQL catalog compatibility without changing live volumes. | Strong: release evidence performs these checks manually. | H | H | Strong | M | L | Completed 2026-08-31; shell-verified |
| CI-003 | Recovery | Add a documented retention report for backup directories before pruning old releases. | Strong: retained release and backup directories are currently operator-managed. | M | M | Strong | S | M | Completed 2026-08-31; the bounded read-only report separates explicit/latest/recent retention, review-only older complete backups, and fail-closed incomplete/unknown entries, defaults to aggregate output, never follows top-level symlinks or deletes data, and passed 4 focused tests |
| CI-004 | Recovery | Record backup schema/tool versions and source SHA in the backup manifest. | Strong: deployment manifest already records image/source metadata separately. | M | M | Strong | S | L | Completed 2026-08-31; source SHA, PostgreSQL tool, and manifest schema recorded |
| CI-005 | Recovery | Add a restore rehearsal fixture for `.cr` archives and attachment assets using disposable volumes. | Strong: `.cr` round-trip is tested, but release restore still needs operational rehearsal. | H | M | Strong | L | L | Closed 2026-09-01 as already covered; `test_system_archive_api.py` exercises temporary storage roots, archive generation/restoration, three attachments, occurrence/RenderBlock/content readback and tamper rejection; `test_cr_archive.py` covers export/import, TOC/search and duplicate policies; full Docker-volume rehearsal remains an operator runbook procedure |
| CI-006 | Recovery | Add an operator command to compare current database attachment references with filesystem assets before deploy. | Strong: artifact lifecycle contract requires stage/validate/publish boundaries. | H | H | Strong | M | M | Completed 2026-08-31; the read-only `scripts.verify_attachment_storage` reconciles active Attachment/AssetObject metadata with local files, validates path boundaries/existence/size and optional SHA-256, reports orphan object files without touching technical staging, fail-closes incomplete scans, and defaults to aggregate output; 4 focused and 430 full API tests passed |
| CI-007 | Recovery | Emit a bounded report for `SAFE_TEMP`, `ORPHAN_FINAL`, and `SUPERSEDED_ARTIFACT` cleanup debt. | Observed: cleanup classifier exists and production has orphan debt. | H | M | Observed | M | M | Completed 2026-08-31; `artifact_cleanup_dry_run` now defaults to a 100k-file aggregate-only report with explicit completeness, scanned/eligible counts and bytes, exits 2 when truncated, and supports opt-in `--fail-on-debt` without exposing paths or tokens; 17 cleanup/lifecycle tests passed with 1 platform skip |
| CI-008 | Recovery | Add a manual, race-safe apply mode for only explicitly approved artifact cleanup candidates. | Strong: automatic cleanup is intentionally disabled; dry-run exists. | H | M | Strong | L | H | Closed 2026-09-01 as already implemented; the cleanup CLI requires category and opaque-token confirmation, performs fresh per-object canonical/path/grace rechecks, reports partial outcomes accurately, and remains manual-only |
| CI-009 | Recovery | Verify backup storage free space and fail before starting a partial backup. | Inferred: current script writes directly without a preflight capacity check. | H | H | Inferred | S | M | Completed 2026-08-31; conservative size preflight added |
| CI-010 | Recovery | Add a backup failure notification hook that contains no conversation content or secrets. | Hypothesis: operators currently discover failures from command output. | M | M | Hypothesis | M | M | Closed 2026-09-01 as unconfirmed; `deploy/backup.sh` already emits bounded failure text and a non-zero exit, but the repository has no authorized notification channel or delivery contract. No external hook is added without operator evidence and configuration. |
| CI-011 | CI | Cache the Web production build output between quality and negative-PWA gates where valid. | Observed: workflow runs Web production build before tests and again for PWA negative matrix. | M | M | Observed | M | M | Closed 2026-09-01 after workflow revalidation; the negative matrix intentionally uses `NEXT_DIST_DIR=.next-pwa-negative` and `NEXT_PUBLIC_PWA_NEGATIVE_TESTS=1`, so reusing the standard `.next` output would be incorrect. The standard and negative builds now remain isolated and timed independently |
| CI-012 | CI | Split API-only and Web-only quality jobs so unrelated failures identify their owner faster. | Observed: one quality job contains both suites and browser services. | M | M | Strong | M | M | Completed 2026-09-01; release and performance workflows now expose separate `api-quality` and `web-quality` jobs; image/characterization jobs require both, while browser integration remains in Web quality with disposable API services |
| CI-013 | CI | Add explicit timeout and artifact collection for every long browser gate. | Strong: first deployment attempt timed out in optional SW readiness. | H | M | Strong | M | L | Completed 2026-08-31; 15/15 gates are bounded and run 33331355359 attempt 2 retained 12/12 passed release-gate records |
| CI-014 | CI | Publish deployable artifact names with commit SHA and run attempt instead of a constant name. | Observed: workflow artifact is always `chat-reader-images`. | M | M | Observed | S | L | Completed 2026-08-31; Actions run 33329145714 verified exact name |
| CI-015 | CI | Add a machine-readable gate summary that distinguishes PASS, NOT VERIFIED, and SKIPPED. | Strong: current reports require manual interpretation of gates. | M | M | Strong | M | L | Completed 2026-08-31; 12-gate summary emits PASS/FAIL/SKIPPED/NOT_VERIFIED without inferring missing evidence |
| CI-016 | CI | Add pnpm store cache hit/miss metrics to release evidence. | Inferred: setup-node caches pnpm but no effectiveness evidence is recorded. | L | L | Inferred | S | L | Completed 2026-09-01; release and performance workflows now restore the pnpm store with an explicit cache key and upload `pnpm-cache.txt` containing hit/miss and lockfile-key evidence. |
| CI-017 | CI | Add a changed-area test selector while retaining the full release gate before deployment. | Inferred: every local cycle runs broad suites even for narrow docs/UI changes. | M | L | Inferred | M | M | Completed 2026-09-01; `scripts/ci/changed-area-selector.mjs` classifies the real diff, emits deduplicated local checks, and always records `full_gate_required: true`; Node built-in tests cover empty and dirty worktrees |
| CI-018 | CI | Upgrade GitHub Actions runtime versions after compatibility verification and record the migration. | Observed: workflow currently uses setup actions with changing Node runtime warnings. | M | M | Strong | S | M | Closed 2026-09-01 as already current; all release and performance workflows use maintained `actions/checkout@v4`, `actions/setup-node@v4`, `actions/setup-python@v5`, `pnpm/action-setup@v4`, and `actions/upload-artifact@v4` (plus `docker/setup-buildx-action@v3`), so no compatibility migration is justified at this SHA |
| CI-019 | CI | Add a release artifact download-and-inspect job independent from the image builder. | Strong: deploy uses downloaded artifact and image inspection is a critical boundary. | H | M | Strong | M | M | Completed 2026-09-01; `inspect-release-artifact` downloads the exact SHA/run-attempt artifact after image build, verifies manifest commit and required files, reloads all four images, and checks OCI revision labels before any downstream consumer can use the package |
| CI-020 | CI | Preserve browser reports per gate with a stable index linking source SHA and environment. | Strong: reports are uploaded, but gate ownership is not centralized. | M | L | Strong | M | L | Closed 2026-09-01 as already implemented; per-gate `gate-evidence.json` plus `gate-summary.mjs` provide stable gate ownership/status and source-SHA metadata, and the release workflow uploads the complete report directory |
| CI-021 | CI / Development | Make API test startup fail clearly or upgrade a disposable database when its Alembic current revision is behind the repository head. | Observed 2026-08-31: the local database was at `20260823_0028` while the repository head was `20260829_0029`, and migration integrity was reported as skipped inside an otherwise green full suite. | H | M | Observed | M | M | Closed 2026-08-31; API tests intentionally use disposable SQLite stores and must not mutate the operator's local PostgreSQL, while the separate dynamic CI verifier now fail-closes repository/current divergence |
| DEP-001 | Deployment | Add a preflight that rejects `http://host:443` and verifies HTTPS redirect/TLS listener behavior. | Observed: screenshot showed plain HTTP sent to HTTPS port; HTTPS root returns 307. | H | H | Observed | S | L | Completed 2026-08-31; external HTTPS verifier added |
| DEP-002 | Deployment | Add a post-deploy check that running container OCI revisions equal the release SHA. | Observed: deployment performs this manually and images are immutable. | H | H | Strong | S | L | Completed 2026-08-31; read-only runtime verifier added |
| DEP-003 | Deployment | Add a post-deploy check that PostgreSQL was not unintentionally restarted during Web/API rollout. | Strong: current deployment explicitly preserves PostgreSQL. | H | M | Strong | S | M | Completed 2026-09-01; read-only `verify_postgres_unchanged.sh` compares Docker `StartedAt` and optional container ID before/after rollout and fails closed on mismatch |
| DEP-004 | Deployment | Move server current-image and rollback state into a documented operator-owned state location. | Observed: `current-images.env` and rollback state are untracked server files. | M | M | Observed | M | M | Completed 2026-09-01; documented `/etc/chat-reader/release-state` ownership and added read-only `deploy/verify_release_state.sh` requiring distinct current/rollback `RELEASE_SHA` values |
| DEP-005 | Deployment | Add release disk-space preflight covering image layers, backup, and transfer directory. | Inferred: King build OOM and storage pressure are known operational risks. | H | H | Strong | S | M | Completed 2026-08-31; production read-only pass and forced-capacity refusal verified on the shared filesystem |
| DEP-006 | Deployment | Add a bounded release transfer cleanup procedure that preserves the active and previous rollback artifacts. | Observed: release transfer directory remains retained after deployment. | M | M | Strong | M | M | Completed 2026-09-01; added explicit-keep, dry-run-by-default `deploy/cleanup_release_transfer.py` with symlink/direct-child safeguards |
| DEP-007 | Deployment | Add a rollback smoke test that checks health, migration head, and anonymous auth boundaries. | Strong: rollback images are retained but checks are manual. | H | M | Strong | M | M | Completed 2026-09-01; read-only `verify_rollback_smoke.sh` combines HTTPS health, anonymous private-route 401 and existing migration-container head verification |
| DEP-008 | Deployment | Pin and periodically verify the external Chromium path used for browser acceptance. | Observed: authenticated browser verification is environment-dependent. | M | M | Strong | S | L | Completed 2026-09-01; `scripts/verify-chromium.mjs` validates an operator-provided absolute executable and Chromium-compatible version before external Playwright runs; CI bundled-browser behavior is unchanged |
| DEP-009 | Deployment | Add an operator runbook for production-equivalent versus production verification labels. | Strong: current evidence distinguishes these levels and avoids overclaiming. | M | M | Strong | S | L | Closed 2026-09-01 as already covered; `DEPLOYMENT_AND_ENVIRONMENT.md` defines PASS/NOT_VERIFIED/BLOCKED, separates production-equivalent owner runs from deployed production evidence, and forbids promoting health-only evidence |
| DEP-010 | Deployment | Add a health check for core services and a recent worker heartbeat after rollout. | Observed: worker liveness is a release gate and production diagnostic. | H | H | Observed | S | L | Completed 2026-08-31; protected runtime health verifier added |
| DEP-011 | Deployment | Make PostgreSQL backup creation and isolated verification consume custom dumps through the supported stdin contract. | Observed: PostgreSQL 16 treated `pg_restore --list -` as a literal filename and refused the first pre-deploy backup attempt. | H | H | Observed | S | L | Completed 2026-08-31; corrected helpers created and independently verified the retained five-component production recovery point |
| OFF-001 | Offline | Make offline package import commit in bounded chunks with resumable progress after IndexedDB abort. | Observed: prior `searchDocuments.bulkPut` aborted thousands of operations. | H | H | Observed | L | H | Closed 2026-08-31 as the current importer already bounds every bulk store write to 100 records inside an atomic replacement transaction; a 15,448-record production-equivalent probe passed as 155 batches (max 100, final 48), and the existing AbortError browser contract preserves the prior package/cache before a successful retry. Durable cross-retry journaling remains separately scoped as OFF-003 |
| OFF-002 | Offline | Add an offline package preflight estimating IndexedDB quota before packaging/import. | Strong: browser quota and eviction variance are documented risks. | H | H | Strong | M | M | Completed 2026-08-31; import preflight now accounts for package JSON structured-clone/index overhead, asset Cache Storage overhead, record indexes, and a bounded fixed reserve before replacing the prior copy; the full 12-test PWA negative matrix proves early QUOTA refusal performs zero IndexedDB writes and preserves the readable package |
| OFF-003 | Offline | Add an import journal that records the last completed store/chunk without storing conversation content. | Inferred: current abort can leave an apparently downloaded package with no usable content. | H | H | Inferred | L | H | Closed 2026-08-31 as not needed by the current atomic contract; every store/chunk is written inside one Dexie replacement transaction, AbortError restores the prior readable package/cache, and the PWA abort/retry matrix proves no partial package is exposed. Durable cross-retry journal state would add persistence without improving the verified recovery path |
| OFF-004 | Offline | Validate that offline Reader message counts and TOC entries are non-zero after package import. | Observed: a successful download opened with `0 / 0` messages. | H | H | Observed | M | M | Completed 2026-08-31; package counts are validated before replacement and PWA-NEG-022 proves rollback |
| OFF-005 | Offline | Keep v1 package reads while writing v2 import diagnostics for schema/store mismatches. | Strong: AGENTS requires v1 compatibility. | H | M | Strong | M | M | Completed 2026-09-01; v1 remains permissive for legacy omitted derived stores, while v2+ validates required store arrays and optional object shapes before any write, returning a bounded `MALFORMED` schema-mismatch message. Dedicated PWA negative Chromium check passed 1/1 |
| OFF-006 | Offline | Add an offline asset URL lifecycle test covering revoke timing after Viewer close. | Strong: offline attachment URLs are released by timed cleanup. | M | M | Strong | M | M | Completed 2026-08-31; focused viewer contract verifies duplicate blob URLs are revoked once, non-blob cache URLs are untouched, and both active/thumbnail offline query cleanup hooks remain wired |
| OFF-007 | Offline | Add a visible retry path when one store fails without discarding stores already imported. | Strong: partial package failure is a known recovery concern. | H | M | Strong | M | M | Implemented locally 2026-09-01; failed download requests are retained in the owning Library surface and expose an explicit Retry action while the atomic importer still preserves the last readable package. Typecheck/lint passed; the full Library browser suite timed out in the local PWA startup environment, so browser acceptance is NOT VERIFIED |
| OFF-008 | Offline | Add low-storage messaging that differentiates quota denial from malformed package data. | Inferred: both currently surface as import failure to users. | M | M | Strong | S | L | Completed 2026-08-31; stable error codes and localized recovery copy verified for quota and malformed packages |
| OFF-009 | Offline | Measure package build and import timings by store and chunk size in redacted diagnostics. | Hypothesis: packaging latency is dominated by one or more unbounded stores. | M | M | Hypothesis | M | L | Completed 2026-09-01; existing offline BackgroundJob progress callbacks now accumulate bounded per-phase durations and expose only `phase_durations_ms` in the terminal result (no body, IDs, paths, or chunk payload). Focused offline package API coverage passed 2/2; production timing collection remains pending |
| OFF-010 | Offline | Add mobile offline acceptance at 375px for Library, Reader, TOC, and attachment access. | Strong: desktop and production-equivalent coverage do not replace real mobile verification. | M | M | Strong | M | L | Implemented locally 2026-09-01; Chromium 1234 at 375px with `APP_ENV=test` passed Library readiness, seeded offline Reader content, TOC Section 20 navigation, and offline attachment access. The test is environment-gated because the normal authenticated PWA shell does not provide the fixture; owner-authenticated/production browser evidence remains NOT VERIFIED |
| LOC-001 | Reader | Return a server-resolved locator DTO for every TOC, search, annotation, attachment, and source reference. | Strong: current audit found divergent DOM/block-index authorities. | H | H | Strong | L | H | Completed 2026-09-01; owner Reader target-first navigation, TOC, Search, Annotation, Attachment, token-scoped Share and local Offline resolution now use the shared locator DTO. Message-only dialogue navigation remains an intentional compatibility fallback. |
| LOC-002 | Reader | Replace DOM text reverse lookup with canonical MessageVersion/RenderBlock offsets. | Strong: rendered Markdown differs from source text and causes misses. | H | H | Strong | L | H | Completed 2026-09-01; server/local resolvers choose current MessageVersion and RenderBlock plus canonical Unicode offsets, while DOM Range is limited to final mounted visual alignment and pulse rendering. |
| LOC-003 | Reader | Preserve attachment `occurrence_key` through Files Panel, resolver, and final highlight. | Observed: front-end previously dropped occurrence identity. | H | H | Strong | M | M | Completed 2026-08-31; online/detail/offline targets share identity+offset builder with same-block regression |
| LOC-004 | Reader | Return all same-block attachment occurrences instead of a single dictionary value. | Observed: single-value `occurrence_by_block` overwrote siblings. | H | H | Observed | M | M | Completed 2026-08-31; current Reader, message-block and Offline projections already group each `(message_version_id, block_index)` to a list and emit ordered `attachmentOccurrences[]`; a focused API regression now places two occurrence identities on the same current block and confirms both owner and public Share Reader responses retain both keys while embedded attachment metadata remains storage-path-safe; 1/1 passed |
| LOC-005 | Reader | Add locator version 2 compatibility and stale/ambiguous/not-found status rendering. | Strong: target locator plan defines these statuses. | H | H | Strong | M | M | Implemented locally 2026-09-01; Reader now retains the body on failed navigation and renders reason-specific stale, ambiguous, deleted/merged, attachment-invalid, and generic recovery copy with retry/message fallback actions. Existing `locator_version: 2` anchor fields and stable version/block compatibility remain in place; server-wide resolver consolidation stays scoped to LOC-001/LOC-002. Browser execution remains NOT VERIFIED without authenticated API/worker |
| LOC-006 | Reader | Batch target context and turn-window loading into one resolver response. | Strong: current flow makes duplicate target/turn requests. | M | M | Strong | M | M | Closed 2026-09-01 as already implemented; target-context resolution returns the center `readerTurn`, `loadCompleteTurnWindow` accepts it as `initialTurn`, and in-flight maps deduplicate concurrent reader-turn/target-context requests. The focused Reader location contract proves the center turn is not fetched twice while previous/next neighbors still load |
| LOC-007 | Reader | Add a bounded mount lease for virtualized target blocks before scrolling. | Strong: target blocks may not be mounted when a locator resolves. | H | M | Strong | M | M | Implemented locally 2026-08-31; Reader acquires a cancellable, bounded `acquireReaderBlockLease` before alignment, re-resolves stable block/occurrence identity after mounting, releases leases on cancellation/failure, and times out without clearing the body; the dedicated virtualized-target browser test was discovered but skipped without authenticated fixture, so runtime browser acceptance remains `NOT VERIFIED` |
| LOC-008 | Reader | Keep Reader content visible on locator failure and expose message-level recovery. | Observed: prior failures could leave confusing empty/failure states. | H | H | Strong | M | L | Implemented locally 2026-08-31; failure preserves the current Reader and exposes exact retry plus a message-only fallback that discards stale block/offset identity instead of guessing text; browser execution remains NOT VERIFIED without a local API/worker |
| LOC-009 | Reader | Replace full-message locate background with a short first-line pulse and reduced-motion marker. | Observed: current locate feedback was visually uncomfortable. | M | M | Observed | S | L | Closed 2026-08-31 as already implemented; exact text targets use the first visible Range line, fallback targets use a 3px message marker, both clear after 720ms or immediately on scroll/pointer input, and reduced-motion uses a bounded static fade; the focused 8/8 Reader contract gate passed; authenticated production interaction remains `NOT VERIFIED` |
| LOC-010 | Reader | Add same-text multi-match tests for Unicode, formulas, Markdown markers, and historical versions. | Strong: these are explicit unresolved locator cases. | H | M | Strong | M | L | Completed 2026-08-31; shared text-anchor matching now has deterministic prefix/suffix coverage for repeated quotes, and numeric canonical code-point offsets are translated to DOM UTF-16 offsets so astral Unicode does not shift ranges; focused Reader location tests passed 2/2 |
| ATT-001 | Attachments | Run a read-only attachment reference integrity report across current and historical MessageVersions. | Strong: legacy merge failures can leave stale occurrence links. | H | H | Strong | M | L | Completed 2026-08-31; the audit now scans text, RenderBlocks and occurrences across current plus historical MessageVersions, reports missing Attachment/current occurrence/block and stale-version links without bodies, and exposes a rollback-only CLI with aggregate global output or explicit single-Conversation identities; 8 attachment/merge tests and CLI help passed |
| ATT-002 | Attachments | Add fail-closed merge validation for missing Attachment, block, occurrence, or cross-conversation references. | Strong: merge plan requires transaction rollback on missing mappings. | H | H | Strong | M | H | Completed 2026-08-31; Conversation Merge now validates every copied source MessageVersion before cloning and rejects inactive/missing/cross-conversation Attachments, missing occurrences, missing blocks and block-reference mismatches; Message Merge shares the validator and returns `422 attachment_integrity_invalid`; worker rollback leaves no target, 17 focused tests and the full API suite (426 passed, 6 skipped) passed |
| ATT-003 | Attachments | Recompute `plain_text`, content hash, RenderBlock text, and sanitized HTML after attachment rewrites. | Strong: prior merge only rewrote selected fields. | H | H | Strong | M | H | Completed 2026-08-31; merged MessageVersions recompute display/plain/hash and rewrite version block JSON plus RenderBlock plain/data/sanitized HTML, and current rewritten hashes are now batch-synchronized back to target Message rows; the focused projection/hash test passed 9/9 and the full API suite passed 426 with 6 skips |
| ATT-004 | Attachments | Preserve multiple occurrences per block in API and Files Panel responses. | Observed: single-value block maps lose siblings. | H | H | Observed | M | M | Closed 2026-08-31; current message, Reader turn, and offline package hydration aggregate links as lists per (message_version_id, block_index), and Files API returns every occurrence |
| ATT-005 | Attachments | Add a repair command only for UUID mappings that are unique within one conversation. | Strong: plan explicitly forbids filename/order guessing. | H | M | Strong | M | H | Completed 2026-08-31; dry-run derives only unique `source_attachment_id` mappings inside one Conversation, apply requires the fresh confirmation token and locked revalidation, ambiguous or unresolved references fail closed, repairs create a new MessageVersion and rebuild Search/TOC; focused coverage passed 2/2 and the full API suite passed 418 with 6 skips |
| ATT-006 | Attachments | Add post-merge consistency checks for Reader, Source Editor, Files, Viewer, Search, TOC, and Offline derivations. | Strong: these surfaces must remain coherent after merge. | H | M | Strong | L | M | Completed 2026-08-31; one real attachment merge fixture now verifies the rewritten identity and occurrence through message-window Reader, message detail/Source data, Files, authorized content/Viewer bytes, Search, TOC, and an asset-bearing Offline v3 package; merge plus Offline coverage passed 15/15, while browser/production acceptance remains `NOT VERIFIED` |
| ATT-007 | Attachments | Display an actionable stale-reference state with message fallback and rescan action. | Strong: audit defines explicit recovery actions. | M | M | Strong | S | L | Implemented locally 2026-08-31; attachment targets carry explicit source identity, fallback no longer clears the stale state immediately, stale/failed banners preserve Reader content and offer exact retry, message fallback, and a truthful Files-reference refetch. Attachment policy contracts passed 7/7 plus lint/typecheck/build; a real owner-data stale click remains NOT VERIFIED |
| ATT-008 | Attachments | Add case-insensitive `cr-asset://UUID` parser tests with query/parenthesis edge cases. | Strong: old replacement was case-sensitive and delimiter-dependent. | M | M | Strong | S | L | Completed 2026-08-31; 3 targeted API tests pass |
| ATT-009 | Attachments | Add regression fixtures for image, file, repeated, historical-version, and merged references. | Strong: required attachment acceptance scenarios are listed. | H | M | Strong | M | L | Closed 2026-08-31 as already covered; image and repeated occurrence flows are exercised by attachment upload/Reader browser fixtures, file and merged references by Conversation/Message Merge API regressions, and historical-version links by the attachment integrity audit/version fixtures |
| ATT-010 | Attachments | Add a user-visible distinction between file metadata/details and direct body navigation. | Strong: current Files Panel mixed these actions. | M | M | Strong | M | L | Closed 2026-08-31 as already implemented; single occurrences expose direct body locate, multiple occurrences expand inline into individually locatable references, and File details remains a separate metadata dialog action; attachment browser coverage confirms locate does not open details |
| IMP-001 | Import | Make `SUPPORTED`, `MAPPABLE`, and `NOT_MAPPABLE` server classifications authoritative in every Import surface. | Strong: current UI previously inferred mapping from resolution status. | H | H | Strong | M | M | Completed 2026-08-31; the required Adaptive Family DTO and all Overview grouping/CTA paths now consume server `handling_class` directly, with the legacy `resolution_status` inference removed; API Adaptive Import tests passed 32/32 and Web lint/typecheck passed, while the conditional full-stack Import browser flow was discovered but skipped because `E2E_IMPORT_FLOW=1` and an isolated API runtime were not available |
| IMP-002 | Import | Prevent a NOT_MAPPABLE input from generating a mapping plan or fake canonical preview. | Strong: explicit product boundary. | H | H | Strong | M | M | Completed 2026-08-31; `handling_class` is now enforced server-side before Mapping preview/save and saved-Profile selection, returning structured `FAMILY_NOT_MAPPABLE` instead of relying on the hidden Web CTA; ordinary documentation and reference-manual fixtures remain NOT_MAPPABLE with an empty mapping draft after direct API attempts; focused 2/2 and full Adaptive Import 32/32 tests passed |
| IMP-003 | Import | Preserve resolved Families when a different input is replaced or excluded. | Strong: mixed-batch flow requires session-local replacement. | H | M | Strong | M | M | Closed 2026-08-31 as already implemented and covered; after one valid Family is mapped, excluding an invalid sibling preserves the valid source and reaches READY, while replacing an invalid sibling reanalyzes the session and reuses the verified format without requiring the completed Family to be mapped again; both focused API contracts passed 2/2 and the full Adaptive Import file passed 32/32 |
| IMP-004 | Import | Add deterministic batch completion with committed/failed/skipped counts and result scope. | Strong: previous batch success opened only the first conversation. | H | H | Strong | M | M | Completed 2026-08-31; the existing Import terminal state reports committed conversation/message totals and warnings, keeps multi-item results in the Import surface, and exposes View imported conversations, Open first, and Close without automatic Reader navigation; asynchronous/partial delayed work remains truthfully owned by global Tasks. A new default mock Chromium contract submits two items, verifies the 2-conversation/4-message scope plus warning and all exits, confirms the URL stays at the opener, then navigates only after explicit Open first; 1/1 passed |
| IMP-005 | Import | Add compact imported-conversation links using existing session/task retention, without ImportBatch persistence. | Strong: explicit no-new-product boundary. | M | M | Strong | M | M | Closed 2026-08-31 as already implemented and now explicitly covered; synchronous completion renders up to eight per-Conversation buttons from `conversation_ids` and global Tasks renders retained Import result links, with ordinary Library fallback for larger sets and no ImportBatch/persistent history object. The default mock Chromium batch contract confirms the second imported Conversation has its own direct entry and passed 1/1 |
| IMP-006 | Import | Preserve original opener on Import close, including Reader, Project, Library, and Unclassified. | Strong: return contract is explicit. | H | M | Strong | M | L | Closed 2026-09-01 as already covered by the portal Import Dialog plus shared `useDialogFocus` previous-focus restoration; Adaptive sessions persist in sessionStorage while the overlay is open, and no new route/state is needed. Authenticated owner browser verification remains NOT VERIFIED |
| IMP-007 | Import | Add a Rescue Skill copy/download path that never uploads source content automatically. | Strong: privacy boundary and static skill resources are current contracts. | M | M | Strong | M | L | Completed 2026-08-31; Rescue uses the Skill Registry/static built-in resources for Chinese and English copy/download, and the dialog only starts a local replacement picker; no source content is sent to an external model |
| IMP-008 | Import | Add file replacement validation that re-analyzes only the replaced input row. | Strong: required rescue flow replaces in the same session. | H | M | Strong | M | M | Completed 2026-08-31; the artifact replacement API reanalyzes only the replaced input while preserving resolved sibling Families, with focused API and Import browser recovery coverage |
| IMP-009 | Import | Add per-Family diagnostics explaining why a document cannot be safely mapped. | Strong: user reported ordinary documents incorrectly entering repair. | H | H | Strong | S | L | Completed 2026-08-31; Import Overview now preserves the input-type label while showing the server-owned reason title and content-safe recovery detail for each non-mappable Family; API 32/32, Web lint/typecheck/build passed, focused browser execution remains NOT VERIFIED without an isolated API/database |
| IMP-010 | Import | Add unknown-format fixtures from `examples` that assert NOT_MAPPABLE rather than repairable. | Observed: examples include non-conversation documents. | H | H | Observed | S | L | Completed 2026-08-31; read-only classification of the supplied desktop examples distinguishes Native v2 (`SUPPORTED`), stable message structures (`MAPPABLE`), and instruction/ordinary documents (`NOT_MAPPABLE`); content-safe API fixtures prove ordinary documents expose `OPEN_RESCUE` with no mapping draft, while zero-byte files fail before session creation; Adaptive Import tests pass 32/32 |
| TASK-001 | Background tasks | Keep one stable global Tasks launcher for import, merge, delete, export, and cleanup. | Strong: monitor is global but was visually owned by Import. | H | H | Strong | M | M | Completed 2026-08-31; the authenticated workspace shell owns one `sidebar-tasks-button` and the focused browser flow confirms the same launcher remains available on Home and Search |
| TASK-002 | Background tasks | Add refresh/re-entry tests that reopen an active task after leaving its origin. | Strong: this is a core task-center contract. | H | H | Strong | M | L | Completed 2026-08-31; a mocked active merge task is reopened from Tasks after Home-to-Search navigation and again after full refresh, without depending on its origin component |
| TASK-003 | Background tasks | Distinguish presentation dismissal from server-side cancellation in every task surface. | Strong: explicit cancellation boundary. | H | H | Strong | S | L | Implemented locally 2026-08-31; Task Center backdrop/panel/escape regression proves dismissal sends no cancel request and explicit cancel remains the only cancellation path; browser run uses mocked owner APIs |
| TASK-004 | Background tasks | Preserve finish-current-item/stop-future-items deletion cancellation semantics in copy and API tests. | Strong: current deletion semantics are intentionally non-transactional. | H | H | Strong | M | H | Completed 2026-08-31; API regression cancels during the first durable deletion boundary, proves that item remains deleted, later items remain intact, and the task reports one processed item without transactional rollback |
| TASK-005 | Background tasks | Expose truthful completed, partial, failed, and retry result actions per task type. | Strong: generic Done wording hides actual outcomes. | H | M | Strong | M | M | Implemented locally 2026-08-31; Task Center distinguishes partial completion, reports completed/failed counts, exposes merge/conversation/import/download result actions from existing payloads, and focused Playwright assertions pass |
| TASK-006 | Background tasks | Add bounded retention telemetry for terminal task results without creating permanent history. | Strong: retention is a current implementation decision. | M | M | Strong | M | L | Completed 2026-08-31; configurable 600-second terminal-result window now keeps recent committed/failed/cancelled tasks available through `/api/tasks/active`, exposes redacted aggregate diagnostics, and adds dismissible re-entry coverage without permanent history |
| TASK-007 | Background tasks | Add idempotent retry tests for cleanup, merge, export, and import tasks. | Strong: worker retry exists and task classes differ. | H | M | Strong | M | M | Completed 2026-08-31; API retry matrix covers cleanup, merge, export, batch delete and import, proving failed tasks queue once and repeated retry requests preserve the queued timestamp/attempt state |
| TASK-008 | Background tasks | Add mobile full-height task surface acceptance while keeping the same global task semantics. | Strong: mobile card is only a shortcut representation. | M | M | Strong | M | L | Completed 2026-08-31; the shared Task Center is a 100dvh focused surface below 640px with safe-area-aware header/content padding, while desktop keeps its bounded dialog; 375x812 browser coverage proves global-sidebar entry, full-height bounds, dismissal without cancellation, and same-task re-entry |
| TASK-009 | Background tasks | Add queue wait and execution latency histograms to protected aggregate diagnostics. | Observed: current diagnostics expose averages but not distributions. | M | M | Observed | M | L | Completed 2026-08-31; the existing bounded 500-row timing sample now reports separate non-overlapping queue-wait and execution buckets at 1/5/15/60/300 seconds while retaining average fields; no new query, identifier, history, or migration was added, and protected diagnostics tests pass 8/8 |
| TASK-010 | Background tasks | Add a worker stale-heartbeat alert threshold tied to existing liveness settings. | Strong: heartbeat is already a release contract. | H | H | Strong | S | M | Closed 2026-08-31 as already implemented; `WORKER_HEARTBEAT_STALE_AFTER_SECONDS` is validated against three heartbeat intervals, diagnostics derives `stale` from that threshold, and the deployment runtime verifier fail-closes unless state is `alive_idle` or `alive_busy` |
| UX-001 | Navigation | Keep Sidebar mounted across Conversation, Project, Recent, Search, and Home soft navigation. | Observed: prior route shells remounted and looked like full refreshes. | H | H | Observed | M | M | Closed 2026-08-31 as implemented by the root `WorkspaceShellBoundary`; owner pages render embedded正文 surfaces while login/share/offline/library retain separate boundaries. A new default mock Chromium contract writes an object probe onto the visible Sidebar DOM, navigates Home → Conversation → Project → Recent → Search through client Links/router, and confirms the same node survives every transition; 1/1 passed |
| UX-002 | Navigation | Add browser-flow assertions that only the正文 surface changes on list navigation. | Strong: soft-navigation acceptance requires DOM/request evidence. | H | M | Strong | M | L | Completed 2026-08-31; the persistent-shell Chromium contract now combines Sidebar DOM identity with network evidence: Home → Conversation → Project → Recent → Search replaces embedded content through App Router navigation while the Sidebar probe survives and only the initial Home load creates a `document` request; 1/1 passed |
| UX-003 | Navigation | Ensure project rows and conversation rows share the same custom-drag affordance without native link drag ghosts. | Observed: project drag still showed a link-like preview. | M | M | Observed | M | L | Closed 2026-08-31 as already implemented and covered by TEST-005; project and conversation links disable/prevent native drag, the full row owns dnd-kit activation, and both custom non-link overlays preserve source dimensions while Escape cancels without mutation. The combined production-equivalent Chromium drag contract was rerun and passed 1/1 |
| UX-004 | Navigation | Keep project order custom-only while preserving existing conversation sort rules and compatibility query fields. | Strong: current product decision explicitly separates these semantics. | M | M | Strong | S | M | Completed 2026-08-31; every Web project query uses `sort=custom&direction=asc`, `create_project()` appends with `max(sort_order)+PLACEMENT_GAP`, conversation sorting remains independently configurable, and the legacy project sort preference/query contract remains readable for compatibility; `test_projects_api.py` passed 10/10 |
| UX-005 | Navigation | Add delayed, collision-aware description previews for truncated project/list rows. | Strong: current UI includes a shared hover preview pattern. | M | L | Strong | M | L | Completed 2026-08-31; shared `HoverPreviewLink` waits 700ms and only presents for truncated copy, flips within viewport bounds, closes on pointer leave/scroll/drag/Escape, provides keyboard focus parity and suppresses touch previews; All Conversations and Project Conversations share the contract, and focused Chromium passed 1/1 |
| UX-006 | Navigation | Make Settings an upward sidebar region with bounded height and internal scroll. | Observed: Settings was too tall/modal-like in screenshots. | M | M | Observed | M | L | Implemented locally 2026-08-31; `SidebarPreferences` renders a bottom-up `role=region` with bounded `min(24rem, 100dvh-8rem)` height and one internal scroll container, keeps More settings inside the bound, and supports Escape/toggle collapse with focus restoration while focused categories retain their existing dialog contract; the existing authenticated Reader layout test was discovered but skipped because its auth fixture is unavailable, so runtime browser acceptance remains `NOT VERIFIED` |
| UX-007 | Navigation | Add an explicit close affordance and Escape behavior to all transient More menus. | Observed: native details menus required re-clicking to close. | M | M | Strong | S | L | Completed 2026-08-31; attachment, annotation, project, conversation, and reader-action menus already use controlled dismissal; the remaining transient sort menu now supports outside click, Escape, `aria-expanded`, and trigger focus restoration, with focused Playwright coverage |
| UX-008 | Navigation | Keep batch mode toggle width stable and label its exit as `完成`/`Done` with an X icon. | Observed: current batch control disappeared after activation. | M | M | Observed | S | L | Completed 2026-08-31; the shared toggle retains its 7.5rem width, switches to X plus `完成`/`Done`, and a focused browser assertion keeps pre/post activation width within 1px |
| UX-009 | Navigation | Make Merge Conversations a dialog or full-width surface independent of the list row clipping boundary. | Observed: merge action was constrained inside a list frame. | M | M | Strong | M | M | Completed 2026-08-31; both conversation lists reuse the body-level `MergeConversationsDialog`, and focused Playwright proves two selected conversations open a visible dialog outside the list frame |
| UX-010 | Navigation | Remove or replace non-actionable hover styles on Project and All Conversations rows. | Observed: users reported rows looking disabled while still clickable. | M | M | Observed | S | L | Completed 2026-08-31; row hover ownership is independent from sortable enablement, so recent-read/non-custom sorting keeps pointer navigation, full opacity and a whole-row subtle hover state in All Conversations and Project Conversations; the focused production-equivalent Chromium contract passed both routes |
| SEC-001 | Security | Add automated assertions that user Skill content is never sent to logs, telemetry, Share, or Offline Catalog. | Strong: Skill security contract explicitly forbids these paths. | H | H | Strong | M | M | Completed 2026-08-31; request-log, public Share, Offline Catalog, and unpacked offline-package regressions exclude uploaded Skill name/content; no application telemetry transport exists |
| SEC-002 | Security | Add owner/share/offline matrix tests for Settings, Tasks, Skills, and attachment controls. | Strong: permission boundaries are explicit and high impact. | H | H | Strong | M | M | Completed 2026-08-31; owner APIs accept the authenticated session, a public Share token only opens its scoped Share, guest attempts against Preferences/Skills/Tasks/attachment management remain 401/no-store, and Offline Settings hides all owner maintenance categories; 24 API tests and the focused Offline browser flow pass |
| SEC-003 | Security | Add a static-resource CSP test for built-in Skill Markdown files. | Strong: built-in resources are served through current CSP/static policy. | M | M | Strong | S | L | Completed 2026-08-31; fixed auth-proxy interception, all four URLs now return Markdown with `nosniff` and a zero-execution sandbox CSP, verified in production-mode Next and Playwright |
| SEC-004 | Security | Reject uploaded Skill files containing invalid UTF-8, binary bytes, empty content, or oversized payloads before persistence. | Strong: upload contract defines all four checks. | H | H | Strong | M | L | Completed 2026-08-31; API regression coverage confirms empty, NUL/binary-like, invalid UTF-8, and >512 KiB uploads are rejected before persistence |
| SEC-005 | Security | Add tests proving Skill previews use text-only rendering and cannot execute HTML/Markdown scripts. | Strong: user configuration is displayed in `<pre>`. | H | M | Strong | S | L | Completed 2026-08-31; static Playwright contract confirms `<pre>` rendering and no Markdown/HTML execution path |
| SEC-006 | Security | Add redaction assertions for request IDs, attachment paths, and error messages in structured logs. | Strong: observability contract requires redaction. | H | M | Strong | M | M | Implemented locally 2026-08-31; structured events now sanitize sensitive keys, Windows/Unix paths, bearer/database URLs, nested values and oversized strings; direct exception logging paths were converted to error-class events, with observability and leak checks passing |
| SEC-007 | Security | Add a regression test that public Share never exposes owner-only backup/security/import-format controls. | Strong: Share-scoped shell boundary is explicit. | H | M | Strong | M | L | Implemented locally 2026-08-31; authenticated Share browser assertion now checks absence of Import, Tasks, and Settings controls, but runtime execution remains NOT VERIFIED without isolated auth credentials |
| SEC-008 | Security | Add secure-cookie and inactivity-timeout browser matrix coverage for production-equivalent auth. | Strong: auth contract and CI gate already define these settings. | H | M | Strong | M | M | Implemented locally 2026-09-01; API tests cover the exact 48-hour inactivity boundary and non-extending session polling, while the authenticated browser gate now asserts HttpOnly, SameSite=Lax and Secure matches the HTTPS base URL; execution remains NOT VERIFIED without an approved owner-authenticated production-equivalent run |
| SEC-009 | Security | Add attachment range-request abuse limits and tests for oversized preview reads. | Inferred: attachment viewers issue bounded Range requests but abuse limits need explicit evidence. | M | M | Inferred | M | M | Completed 2026-08-31; owner/share/derivative content responses share an 8 MiB per-Range cap, preserve full non-Range downloads, and return stable 416 responses for multi-range, empty, oversized-numeric, or out-of-bounds input; 24 attachment/share tests pass |
| SEC-010 | Security | Add a migration-time check that no new database head is introduced by feature work. | Strong: single Alembic head is mandatory. | H | M | Strong | S | L | Implemented locally 2026-08-31; release and performance CI now call one dynamic verifier that rejects multiple repository heads and a database not exactly at that head, replacing stale hard-coded revisions; repository-only and refusal paths verified locally, CI runtime pending |
| PERF-001 | Performance | Track Reader first-content, locator resolution, and target-mount latency separately. | Strong: current locator delay is user-visible and conflated with rendering. | H | H | Strong | M | L | Completed 2026-08-31; the Reader emits bounded browser Performance/CustomEvent evidence for first content, locator resolution, and target mount with success/fallback/failure/cancellation plus local/remote path enums, and never includes object IDs or user content; lint, typecheck, production build, and the 7/7 Reader contract browser gate passed; production timing collection remains `NOT VERIFIED` |
| PERF-002 | Performance | Add a locator fast path keyed by message version and render block before quote remapping. | Strong: exact identity is available in current APIs. | H | H | Strong | M | M | Implemented locally 2026-08-31; mounted exact current-version/block targets skip target-context/neighborhood requests and use a short layout/media settle window, while network/remapped targets retain conservative waits; lint/typecheck/build and Playwright discovery pass, long Reader browser assertion remains NOT VERIFIED without a local API/worker |
| PERF-003 | Performance | Bound large Reader virtualization work by block count and character count with measured thresholds. | Strong: large-conversation risk and current virtualization contract are documented. | H | M | Strong | M | M | Closed 2026-08-31 as already implemented; `AssistantMessageRenderer` virtualizes a message when `block_count > 160` or `char_count > 50,000`, while the Reader window caps hydrated messages at 120 and existing long-message browser fixtures verify bounded mounted rows and non-overlapping virtual blocks |
| PERF-004 | Performance | Measure attachment Viewer Range throughput and retry cost by media type. | Inferred: viewers use multiple bounded reads and retries. | M | M | Inferred | M | L | Implemented locally 2026-09-01; `scripts/performance/measure-attachment-ranges.mjs` and its Node test characterize image/PDF/video/text bounded Range reads plus retry counts using synthetic content, with aggregate-only output; production media corpus and network throughput remain NOT VERIFIED |
| PERF-005 | Performance | Add API query timing buckets for conversation, TOC, search, and Files Panel endpoints. | Observed: code logs some mutation timings but not a uniform endpoint view. | M | M | Strong | M | L | Completed 2026-08-31; request completion events now add bounded endpoint-family labels and fixed duration buckets for conversation/list/reader, TOC, search, and conversation attachment reads without query strings, IDs, or content; focused observability tests pass |
| PERF-006 | Performance | Avoid duplicate Reader target-context and turn-window requests through a combined response. | Strong: current audit identifies duplicate loading. | M | M | Strong | M | M | Completed 2026-08-31; target navigation now reuses `ReaderTargetContext.readerTurn` as the complete-window center and fetches only adjacent turns, preserving existing cache/API contracts; the focused Reader window test confirms no center refetch and passed 1/1, with lint/typecheck still green |
| PERF-007 | Performance | Add offline packaging progress by store so `packaging` is not an opaque long state. | Observed: packaging can remain long without useful progress. | M | M | Observed | M | L | Completed 2026-08-31; offline package jobs now report message, headings, search, annotations, metadata, attachment index, conversation, asset-write, validation, and publishing phases through the existing BackgroundJob callback; browser Task Center coverage displays the current store and progress without adding task history |
| PERF-008 | Performance | Compare Next Web build memory and duration before/after cache changes in CI artifacts. | Observed: Web build uses a 2048 MB heap and is repeated. | M | M | Strong | S | L | Completed 2026-09-01; release quality uploads source-bound elapsed-time and peak-RSS files for standard and PWA-negative Web builds, providing a baseline/comparison artifact without changing build outputs |
| PERF-009 | Performance | Add a bounded search index bulk-write benchmark for 15k-document imports. | Observed: prior IndexedDB failure involved 15,448 operations. | H | H | Observed | M | M | Closed 2026-08-31 as already implemented; offline Dexie writes use 100-record `bulkPut` chunks and the PWA negative matrix verifies 15,448 records become 155 batches with a maximum of 100, alongside abort rollback and idempotent retry coverage |
| PERF-010 | Performance | Add production diagnostic percentiles for queue wait, execution, and artifact packaging duration. | Observed: diagnostics currently expose averages such as 5.02s queue and 19.675s execution. | M | M | Observed | M | L | Completed 2026-08-31; the existing capped 500-row task timing sample now reports fixed p50/p95/p99 queue-wait and execution percentiles alongside averages/histograms, with empty-safe pure-function and protected diagnostics assertions |
| TEST-001 | Testing | Add authenticated browser acceptance for the current deployed SHA using disposable owner credentials. | Observed: production owner-authenticated UI remains NOT VERIFIED. | H | H | Observed | M | M | Candidate |
| TEST-002 | Testing | Add desktop/mobile tests for direct attachment locate with single and multiple occurrences. | Strong: prior flow opened details instead of navigating. | H | H | Strong | M | L | Completed 2026-08-31; desktop/mobile single and repeated occurrence flows passed without opening file details |
| TEST-003 | Testing | Add browser tests for More-menu outside click, Escape, and focus restoration in Files and Annotations. | Strong: inconsistent menu contracts were observed. | M | M | Strong | M | L | Completed 2026-08-31; Files Playwright PASS, annotation menu focus/Escape behavior verified before environment cleanup timeout |
| UX-011 | Reader UX | Make mobile message actions a controlled sheet with outside-click/Escape dismissal and trigger focus restoration, and add independent annotation/failed-location regression coverage. | Strong: mobile actions still used native details and the previous annotation evidence was coupled to a large upload flow. | H | M | Strong | M | L | Implemented locally 2026-08-31; lint/typecheck/build and Playwright discovery pass, browser execution NOT VERIFIED because local Chat Reader API/worker is unavailable |
| UX-012 | Batch operations | Do not propagate disabled sortable semantics to interactive row descendants while batch selection is active. | Observed 2026-08-31: `useSortable({ disabled: true })` still spread `aria-disabled=true` onto both conversation-list wrappers, causing browsers to treat their checkboxes and selection buttons as disabled. | H | H | Observed | S | L | Completed 2026-08-31; DnD attributes/listeners are now omitted when sorting is disabled in All Conversations and Project Conversations, and the body-level merge browser flow selects two rows successfully |
| TEST-004 | Testing | Add browser tests for project/list row hover preview timing and viewport collision. | Strong: delayed preview contract is explicit. | M | L | Strong | M | L | Completed 2026-08-31; production-equivalent Chromium verifies no preview at 500ms, presentation after 700ms, full 480x280 viewport containment, Escape dismissal, pending-timer cancellation on scroll, touch suppression, and the same contract on All Conversations and Project Conversations |
| TEST-005 | Testing | Add browser tests that drag overlays preserve source row dimensions within 2px. | Strong: source-sized overlay is a product acceptance requirement. | M | M | Strong | S | L | Completed 2026-08-31; production-equivalent Chromium first reproduced a 14px project-overlay height mismatch when dnd-kit supplied no initial rect, then passed after all project/conversation list overlays gained a translated/real-DOM size fallback; project and conversation overlays stay within 2px and Escape cancellation emits no reorder mutation |
| TEST-006 | Testing | Add browser tests that Reader locate pulse never clears正文 on stale/not-found results. | Strong: failure must preserve current content. | H | M | Strong | S | L | Completed 2026-08-31; mock Chromium covers an out-of-range block target and a 404 target message, keeps the safe Reader article mounted, exposes retry/message recovery for the failed target, and confirms no locate pulse remains after failure; 2/2 passed, while authenticated production stale-version migration remains NOT VERIFIED |
| TEST-007 | Testing | Add merge attachment consistency tests spanning API, Source Editor, Reader, Files, Viewer, Search, and TOC. | Strong: merge regressions affected multiple surfaces. | H | H | Strong | L | M | Closed 2026-08-31 as already covered by the current merge regression suite; `test_merge_history_and_cancellation.py` plus `test_split_merge_api.py` passed 17/17, including attachment rewrite/occurrence integrity, RenderBlock/hash projections, source immutability, message/detail/Source data, Files, authorized Viewer bytes, Search, TOC, Offline package output, and fail-closed rollback. Authenticated browser/production verification remains NOT VERIFIED |
| TEST-008 | Testing | Add offline package tests for v1 read, v2 import, abort/resume, and zero-message detection. | Strong: offline compatibility and prior empty Reader are evidenced. | H | H | Strong | L | M | Completed 2026-08-31; production-equivalent Chromium imported a legacy v1 package and rendered its message through the real Offline Reader, rejected a v2 package whose declared message count had no stored messages while preserving the prior readable copy, and retained the existing package/cache across an injected IndexedDB abort before an idempotent retry; 3/3 focused checks passed, with cold-start coverage remaining in the existing Library PWA suite |
| TEST-009 | Testing | Add a test inventory check that every feature directory has a named test owner or explicit reason. | Inferred: feature inventory and E2E coverage are uneven. | M | L | Inferred | M | L | Completed 2026-09-01; `feature-test-inventory.spec.ts` enumerates every Web feature directory and requires a named test owner or shared-contract reason; Playwright 1/1 passed |
| TEST-010 | Testing | Add a release evidence index linking each required command to its output artifact and verification level. | Strong: release reports distinguish automated and browser evidence. | M | M | Strong | M | L | Completed 2026-09-01; `RELEASE_EVIDENCE_INDEX.md` maps quality, migration, browser and production gates to source-bound evidence and explicitly preserves NOT_VERIFIED semantics |
| DOC-001 | Documentation | Update `docs/index.md` current verification date after each production deployment. | Observed: index had an older current verification date than the deployment. | L | M | Observed | S | L | Completed 2026-09-01; current index date is aligned to the latest recorded 2026-08-31 deployment evidence, while this cycle itself was not deployed |
| DOC-002 | Documentation | Keep `PROJECT_STATE.md` under the compact snapshot target and link detailed release evidence instead of duplicating logs. | Strong: documentation skill sets a 100–250 line target; current file is large. | M | M | Strong | M | M | Completed 2026-09-01; condensed `PROJECT_STATE.md` to the current-state snapshot and preserved the previous 1,862-line history under `docs/archive/` |
| DOC-003 | Documentation | Add the continuous backlog to the docs index and inventory with lifecycle ownership. | Strong: durable docs must be discoverable. | M | M | Strong | S | L | Closed 2026-08-31 as already implemented; `docs/index.md` links the living register and `docs/documentation-inventory.md` assigns its current lifecycle responsibility |
| DOC-004 | Documentation | Add a current troubleshooting entry for IndexedDB bulkPut AbortError and recovery steps. | Observed: user encountered this exact error. | M | H | Observed | S | L | Completed 2026-08-31; current recovery order documented |
| DOC-005 | Documentation | Add a current troubleshooting entry for HTTP-on-HTTPS-port 400 errors. | Observed: screenshot and deployment diagnosis identify this protocol error. | L | M | Observed | S | L | Completed 2026-08-31; protocol diagnosis and verifier documented |
| DOC-006 | Documentation | Record the attachment reference repair boundary and manual-review cases without storing real IDs or正文. | Strong: repair plan forbids guessing and sensitive data persistence. | H | M | Strong | S | L | Completed 2026-08-31; the current troubleshooting contract now distinguishes aggregate global audit output from temporary single-Conversation identities, forbids copying real IDs/body text into durable docs or ordinary logs, enumerates missing/ambiguous/conflicting evidence that requires manual review, and requires backup, new-version repair, post-audit plus Reader/Source/Files/Viewer/Search/TOC verification; unresolved evidence remains NOT VERIFIED rather than guessed |
| DOC-007 | Documentation | Document the exact current retention window for terminal task results and offline artifacts. | Inferred: retention is used by re-entry contracts but not surfaced in one place. | M | M | Inferred | S | L | Completed 2026-08-31; current [Task And Offline Retention Contract](RETENTION_CONTRACT.md) records the 600-second configurable terminal-result view, canonical Offline replacement/no-TTL semantics, 24-hour cleanup grace, browser-managed Library lifetime, and separate Export/draft/upload TTLs; linked from docs index and inventory |
| DOC-008 | Documentation | Add a release checklist item for production owner-authenticated browser status. | Observed: this remains a recurring verification debt. | M | M | Observed | S | L | Completed 2026-09-01; `DEPLOYMENT_AND_ENVIRONMENT.md` now requires an explicit PASS/NOT_VERIFIED/BLOCKED status with owner-session, exact-SHA, and credential-boundary evidence |
| DOC-009 | Documentation | Add a data-boundary map for Reader, Share, Offline, Skills, and `.cr` pipelines. | Strong: multiple boundaries must not be merged during maintenance. | M | M | Strong | M | L | Completed 2026-09-01; `BACKEND_AND_API.md` now records each surface's owned data, forbidden crossings, and explicit projection boundaries without storing user content |
| DOC-010 | Documentation | Add a decision record for keeping automatic artifact cleanup disabled until race-safe evidence exists. | Strong: this is a documented product decision needing durable rationale. | M | L | Strong | S | L | Closed 2026-09-01 as already implemented; `RETENTION_CONTRACT.md` records the 24-hour race-safety grace, dry-run-only discovery, explicit category/token confirmation and fresh recheck, with automatic deletion disabled |

## Prioritization Rules

1. Revalidate `Observed`/`Strong` candidates against the current source SHA before
   implementation; a later fix closes the item instead of creating duplicate
   work.
2. Prefer H-impact/H-urgency items that protect data integrity, recovery,
   authentication, or user-visible loss of context.
3. Do not implement a `Hypothesis` item until a focused measurement confirms the
   underlying cost.
4. Each implementation cycle adopts one independent item, adds a regression
   check, records verification level, and updates this table.
5. `Blocked` means the item needs an external decision, credential, fixture, or
   production capability; blocked work does not block unrelated candidates.

## Initial Counts

| Metric | Count |
|---|---:|
| Discovered candidates | 134 |
| Completed in this backlog | 132 |
| Blocked | 0 |
| Remaining candidates | 2 |
