openapi: 3.1.0
info:
  title: chat-reader API
  version: 0.1.0
paths:
  /api/imports/preview:
    post:
      summary: Preview uploaded source files before import
      responses:
        '200': { description: Import preview }
  /api/imports/{import_id}/commit:
    post:
      summary: Commit a previewed import into canonical conversation storage
      parameters:
        - name: import_id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200': { description: Import committed }
  /api/imports/{import_id}/source-artifacts:
    get:
      summary: List raw source artifacts for an import
      parameters:
        - name: import_id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200': { description: Source artifact list }
  /api/imports/{import_id}/warnings:
    get:
      summary: List import warnings
      parameters:
        - name: import_id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200': { description: Import warnings }
  /api/conversations:
    get:
      summary: List conversations
      responses:
        '200': { description: Conversation list }
  /api/conversations/{conversation_id}:
    get:
      summary: Get conversation detail
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200': { description: Conversation detail }
  /api/conversations/{conversation_id}/messages:
    get:
      summary: Get paginated messages
      parameters:
        - name: conversation_id
          in: path
          required: true
          schema: { type: string }
        - name: cursor
          in: query
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, default: 30 }
        - name: include_blocks
          in: query
          schema: { type: boolean, default: false }
      responses:
        '200': { description: Message page }
  /api/messages/{message_id}:
    get:
      summary: Get message detail
      parameters:
        - name: message_id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200': { description: Message detail }
  /api/messages/{message_id}/blocks:
    get:
      summary: Get render blocks for a message
      parameters:
        - name: message_id
          in: path
          required: true
          schema: { type: string }
        - name: start
          in: query
          schema: { type: integer, default: 0 }
        - name: limit
          in: query
          schema: { type: integer, default: 50 }
      responses:
        '200': { description: Render blocks }
